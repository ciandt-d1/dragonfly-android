<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/component_download_button_background_1"
    android:gravity="center">

    <ImageView
        android:id="@+id/iconStart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/labelStart"
        android:layout_alignBottom="@+id/labelStart"
        android:contentDescription="@null"
        android:scaleType="center"
        android:src="@drawable/ic_download"/>

    <TextView
        android:id="@+id/labelStart"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_toEndOf="@+id/iconStart"
        style="@style/ProjectSelection.Item.Download"
        android:textAllCaps="true"
        tools:text="Download"
    />

</RelativeLayout>